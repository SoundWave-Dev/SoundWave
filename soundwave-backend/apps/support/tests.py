# TODO(Foad): minimum tests for this app:
# - a listener can create a ticket and only sees their own tickets
# - a support user sees all tickets
# - approving an artist verification request sets ArtistProfile.verification_status=approved
# - rejecting requires a `reason` and stores it
