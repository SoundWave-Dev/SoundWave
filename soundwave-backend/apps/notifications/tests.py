# TODO(Foad): minimum tests for this app:
# - a user only sees their own notifications
# - mark-read flips is_read on one notification
# - mark-all-read flips is_read on every unread notification for that user
